#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;

int n;
int want;
vector<int> n_arr;

int main() {

	cin >> n >> want;

	int temp;
	for (int i = 0; i < n; i++) {
		cin >> temp;
		n_arr.push_back(temp);
	}

	sort(n_arr.begin(), n_arr.end());
	
	int low(1);
	int high = 

	while (low <= high) {

	}

	return 0;
}